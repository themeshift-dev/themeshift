import type { ComponentMeta } from '@themeshift/docs-types';

export const meta = {
  category: 'layout-utilities',
  description:
    'Renders children into a detached DOM container for overlays and floating UI.',
  tags: ['portal', 'overlay', 'layering', 'layout'],
  order: 34,
  status: 'experimental',
  related: ['Tooltip', 'Navbar', 'FocusLock'],
} satisfies ComponentMeta;
