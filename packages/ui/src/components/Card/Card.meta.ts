import type { ComponentMeta } from '@themeshift/docs-types';

export const meta = {
  category: 'data-display',
  description:
    'Provides structured, composable content containers with optional media and actions.',
  tags: ['card', 'layout', 'container', 'panel'],
  order: 30,
  status: 'stable',
  featured: true,
  related: ['Heading', 'Button', 'Badge', 'Field'],
} satisfies ComponentMeta;
